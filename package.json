{
  "name": "lorenzo-dong-breaker-pages",
  "private": true,
  "version": "1.0.0",
  "description": "GitHub Pages wrapper for the Lorenzo birthday Arkanoid clone",
  "scripts": {
    "build": "npm run check",
    "check": "node scripts/check-index.mjs",
    "preview": "python3 -m http.server 4174",
    "deploy:help": "bash scripts/deploy-gh-pages.sh"
  }
}
